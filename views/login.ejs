<% if (typeof user === 'undefined') user = null; %>
<%- include('partials/header', {title: 'Login - Bengkel Lam Jaya Motor', user: user}) %>

<div class="row justify-content-center">
    <div class="col-md-6">
        <div class="card">
            <div class="card-header text-center">
                <h4 class="card-title mb-0">🔐 Login</h4>
                <small class="text-muted">Bengkel Lam Jaya Motor</small>
            </div>
            <div class="card-body">
                <!-- Success Message jika signup berhasil -->
                <% if (typeof signup_success !== 'undefined' || (typeof query !== 'undefined' && query.signup_success)) { %>
                <div class="alert alert-success">
                    ✅ Signup berhasil! Silakan login dengan akun Anda.
                </div>
                <% } %>

                <!-- Error Message -->
                <% if (error) { %>
                <div class="alert alert-danger"><%= error %></div>
                <% } %>
                
                <form method="POST" action="/login" id="loginForm">
                    <div class="mb-3">
                        <label for="username" class="form-label">Username</label>
                        <input type="text" class="form-control" id="username" name="username" 
                               placeholder="Masukkan username" required minlength="3">
                        <small class="text-muted">Minimal 3 karakter</small>
                    </div>
                    <div class="mb-3">
                        <label for="password" class="form-label">Password</label>
                        <input type="password" class="form-control" id="password" name="password" 
                               placeholder="Masukkan password" required minlength="5">
                        <small class="text-muted">Minimal 5 karakter</small>
                    </div>
                    <button type="submit" class="btn btn-primary w-100">Login</button>
                </form>
                
                <div class="mt-4 text-center">
                    <hr>
                    <p class="mb-2">Belum punya akun?</p>
                    <a href="/signup" class="btn btn-outline-primary w-100">
                        Daftar Sekarang
                    </a>
                </div>

                <div class="mt-4 text-center">
                    <small class="text-muted">
                        <strong>Default login untuk testing:</strong><br>
                        Admin: admin / 12345<br>
                        Montir: montir / montir123<br>
                        Visitor: visitor / visitor123
                    </small>
                </div>
            </div>
        </div>
    </div>
</div>

<script>
// Client-side validation
document.getElementById('loginForm').addEventListener('submit', function(e) {
    const username = document.getElementById('username').value.trim();
    const password = document.getElementById('password').value;
    
    if (username.length < 3) {
        e.preventDefault();
        alert('Username minimal 3 karakter!');
        return false;
    }
    
    if (password.length < 5) {
        e.preventDefault();
        alert('Password minimal 5 karakter!');
        return false;
    }
});
</script>

<%- include('partials/footer') %>